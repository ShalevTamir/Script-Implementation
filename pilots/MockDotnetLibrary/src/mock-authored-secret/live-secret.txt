Never leave the building. This must never appear in export output.
Exercises an exclusions entry authored using the project's mock name
("OpenDotnetLibrary/...") as its prefix instead of the real name
("MockDotnetLibrary/...") - both must resolve to this folder.
